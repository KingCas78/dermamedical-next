module.exports = {
  theme: {
    extend: {
      colors: {
        brand: {
          blue: "#2563eb",      // azul principal
          blueLight: "#dbeafe", // azul muy suave
          pink: "#f9a8d4",      // acentos cálidos
          gray: "#4b5563",      // texto principal
          grayLight: "#9ca3af", // texto secundario
        }
      }
    }
  }
};
